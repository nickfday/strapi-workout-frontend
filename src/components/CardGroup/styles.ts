import styled from 'styled-components';
import CardMedia from '@material-ui/core/CardMedia';

const StyledLinkWrapper = styled.div`
  text-align: center;
`;

const StyledCardMedia = styled(CardMedia)`
  height: 250px;
`;

export { StyledLinkWrapper, StyledCardMedia };
